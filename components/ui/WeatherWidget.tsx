import { WeatherWidgetSimple } from './WeatherWidgetSimple';

/**
 * WeatherWidget - A draggable weather widget
 * 
 * Re-exports the simplified implementation which avoids the ToastContext issues
 */

export const WeatherWidget = WeatherWidgetSimple;
